
#include "Utilities.h"

namespace OMG
{
	/*
	template<typename T>
	T* Utilities::GetAtIndex(list<T*> _list, int _index)
	{
		list<T>::iterator it = _list.begin();
		for (int i = 0; i < _index; i++) {
			++it;
		}

		return *it;		
		
	}*/

}